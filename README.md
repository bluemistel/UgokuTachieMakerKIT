# 動く立ち絵MakerKIT 「ウゴメキ」(ugomeki)

PSD/PSBファイルをゆっくりMovieMaker4 (YMM4) の動く立ち絵形式に変換・管理するためのデスクトップアプリです。

## 主な機能

- **PSD/PSBファイルの直接読み込み**: PSDまたはPSBファイルをドラッグ＆ドロップするだけで内部を解析してプレビュー
- **動く立ち絵の出力形式に最適化したカテゴリ仕分け(Mapping)**: 眉、目、口、体、髪などのパーツをドラッグ＆ドロップだけで簡単に分類
  - 各レイヤーはCtrl、またはShiftを押しながら複数選択が可能
  - フォルダ単位でドラッグ&ドロップすると一括で自動命名+一括分類が可能(2階層以上は誤登録防止のため未対応)
- **結合モード (Composite)**: 複数のレイヤーを1つのパーツとして結合して書き出し
  - YMM4の体パーツのようにどの部位でも、複数レイヤーを1枚に結合した見た目として出力可能
  - 結合モード中のグループをクリックすることで結合後の見た目を即座にプレビューで確認可能
- **PSDTool記法対応**: `!` (強制表示)、`*` (ラジオボタン)、`:flip` (反転) などの独自記法をUI上で再現
  - 各記法は絵文字アイコンによって置き換えて視覚的に分かりやすく表示
- **リアルタイムプレビュー**: 
  - 仕分け中のパーツの組み合わせを各スライダーで確認
  - ツリー上のチェックボックスで個別に表示/非表示を切り替えられるので細部の確認にも最適
- **一括書き出し**: YMM4が読み込み可能なフォルダ構造とファイル名で最適化して動く立ち絵の形式として出力

## 使い方

1. 「ウゴメキ x.x.x.exe」を起動し、PSD/PSBファイルを中央のキャンバスにドラッグ＆ドロップします。
2. 左側のレイヤーツリーから素材を選択し、右側の「カテゴリ仕分け」エリアへドラッグして分類します。
3. プレビュー画面でスライダーを動かし、意図通りにパーツが切り替わるか確認します。
4. 「YMM4形式で書き出し」ボタンを押し、出力先を設定して書き出します。
5. 出力結果はYMM4上の「動く立ち絵」としてキャラクター設定に反映が可能です。

## 注意事項

本アプリの不具合により何らかの損害が発生した場合でも、作者は一切の責任を負いません。自己責任でのご使用をお願いいたします。

## ライセンス

MIT License
